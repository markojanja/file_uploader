<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body>
    <%- include("partials/header") %>
    <div class="form-container">
      <h1>Create folder</h1>
      <form
        action="/dashboard<%= parentId ? '/' + parentId + '/create' : '/create' %>"
        method="POST"
        class="form"
      >
        <label for="folder">Folder name:</label>
        <input type="text" placeholder="folder" name="folderName" />
        <button class="btn-primary-sm">create</button>
      </form>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
